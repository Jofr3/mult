# Every path in this repository is owned by the maintainer. GitHub requests a
# review from the owner automatically on any pull request touching a match.
#
# Later entries win, so the more specific blocks below narrow nothing today —
# they exist so ownership can be split without rediscovering the risky paths.

* @Jofr3

# Wire protocol and daemon: a change here can break every already-running
# client. See docs/DAEMON.md.
/crates/protocol/ @Jofr3
/src/bin/mult-server.rs @Jofr3

# Durable state, config, path resolution and spawned-command handling: the
# security-sensitive surface described in SECURITY.md.
/src/storage.rs @Jofr3
/src/config.rs @Jofr3
/src/paths.rs @Jofr3
/src/transcript.rs @Jofr3

# Anything that runs in CI or ships a binary.
/.github/ @Jofr3
/flake.nix @Jofr3
/deny.toml @Jofr3
/rust-toolchain.toml @Jofr3
/justfile @Jofr3
