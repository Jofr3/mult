# mult — improvement backlog (v2, rebased)

Start at [ROADMAP.md](ROADMAP.md) — it is the entry point for this list, the
execution order in [PLAN.md](PLAN.md), the standing rules, and the open design
decisions that are not yet items here.

The v1 audit (`docs/BACKLOG-v1.md`) was run against commit `0224885`. `main` had meanwhile
advanced to `2a91632` with ~14.7k lines of independent, overlapping rework. Every v1 item was
re-triaged against the current code; this file is the result, plus 15 findings the v1 audit
never saw because the code did not exist yet.

**Do not read v1 as current.** It is retained only as evidence for items that carried over.
The v1 fixes live on branch `hardening/audit-remediation` (`f451667`) and can be consulted
with `git show hardening/audit-remediation:<path>` — but many no longer apply as written.

Severity: **C** critical · **H** high · **M** medium · **L** low.
Status: `todo` · `wip` · `done` · `dropped`.

## Triage outcome

| | count |
|---|---|
| v1 items **already fixed** by current `main` | 17 |
| v1 items **obsolete** (code restructured away) | 3 |
| v1 items **still applying** (some in changed form) | 82 |
| v1 items **deliberately abandoned** (v1's remedy is now wrong) | 1 |
| **new** findings from this triage | 15 |

## Decisions recorded

- **F1 — abandoned, do not port.** v1 proved the `agent.rs` / `ChatBuffer` / `ProjectState::messages`
  transcript path unreachable and deleted ~700 LOC. That analysis still holds — there is no
  `dyn AgentBackend`, so the call graph is closed and `send_prompt` has no production caller.
  But `main` now builds on those types: `src/transcript.rs` (a versioned, size-capped,
  truncation-recovering append-only journal) needs `ChatMessageRole`, and
  `docs/REMAINING_WORK.md` records it as a **completed Phase 3 deliverable** with Phase 3.4
  planning to feed it and §6.3 listing the dead-code question as an *open* decision.
  Merging v1 would have broken the build and destroyed finished work. The residual action is
  documentation honesty only — see E12.
- **C1 — `main`'s fix supersedes v1's.** `main` never relaunches a persisted
  `TerminalLaunch::Command` (`recoverable_terminals` + `terminal_requires_recovery`). v1
  prompted and then replayed, and that prompt was itself found broken by review. Keep `main`'s.
- **A3 / C12 — `main`'s `SessionIdentity` supersedes v1's instance token.** A 128-bit
  `StateNamespace` plus 128-bit per-session `SessionToken` already provides both the
  cross-instance namespacing (A3) and the per-session capability check (C12). Do not add a
  second mechanism.
- **`PROTOCOL_VERSION` — both reached `10`, meaning different wire shapes.** `main`'s 10
  (`u16`) is authoritative. Any wire change here renumbers from that, and all wire changes in
  this effort must land in a **single** bump, not one per slice.
- **F11 — same name, opposite purpose.** `main`'s `storage::StateStore` is a concrete
  process-lifetime *ownership lock*. v1's was a *testability* trait. Do not shadow or replace
  the lock. The real defect is narrower — see B16.

---

## A. Daemon (server side)

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| A2 | C | `mult-server.rs:1656-1658,2752-2758`; `pty.rs:1676-1689` | **Worse than v1 described.** `handle_leased_input` does a blocking `write_pty_input` (`write_all`+`flush`) to the PTY master **while holding both the global `ServerState` mutex and the pane mutex**. A child that stops reading stdin now freezes the *entire daemon* — every pane, every client — not just one connection. Client side still blocks on `write_message` from the render thread. Needs a per-pane writer thread with a bounded queue **and** the locks dropped before writing. | done |
| N1 | C | `mult-server.rs:1479-1524,1551-1596` | Attach replay runs with the global server mutex **and** the pane mutex held, chunking up to 32 MiB into 512 × 64 KiB `to_vec`s plus channel sends. One client attaching to a busy pane serializes the whole daemon for the duration. Overflow still `disconnect()`s the client it just attached → reconnect loop. (Supersedes A7.) | done |
| N2 | H | `mult-server.rs:235-244` | Daemon shutdown can hang forever: after `begin_daemon_shutdown` the main thread spins on `sessions.is_empty()` with a 20 ms sleep and **no deadline**. A pane whose stop driver reports `TimedOut` is never removed, so `mult-server` never exits and never unlinks its socket. | done |
| A1 | H | `mult-server.rs:2674-2698` | `append_raw_history_with_limit` still `extend_from_slice` + `drain(..overflow)` on a flat 32 MiB `Vec<u8>` — O(history) memmove per 8 KiB read under the pane lock. Replay at `:1573` iterates `raw_history.chunks()`, so a chunk deque changes that path too. | done |
| N3 | H | `mult-server.rs:1551-1596` | Per-attach memory amplification: 32 MiB of history becomes ~512 queued `ReplayChunk` `Vec`s resident in the client's 1024-slot channel *simultaneously*, on top of the pane's own 32 MiB — ~64 MiB per attaching client per pane. | done |
| A8 | M | `mult-server.rs:2160` | `broadcast_foreground_process_if_changed` still called after **every** 8 KiB read: pane lock + master lock + `tcgetpgrp` ioctl + `/proc/<pid>/cmdline`. The debounced `schedule_foreground_process_poll` already exists and is used from the input path — the reader-thread call just needs deleting. Trivial now. | done |
| A10 | M | `mult-server.rs:216-226,937,320-350` | No client cap, no session cap, read timeout cleared after `Hello`, no idle deadline, no keepalive. A same-uid `CreateSession` loop exhausts memory/PIDs/fds. Keepalive is a wire addition. | todo |
| A11 | M | `mult-server.rs:401-412,2566-2583` | `PaneId`/`SessionId` still distinct and always equal, and `pane_by_id`'s dead linear-scan fallback takes **every** pane mutex *while holding the server lock*, on every `Input`/`Resize`/`Detach`. With A2 that is a deadlock multiplier, not just waste. **Scan deleted; newtypes kept:** `PaneId`/`SessionId` are `mult_protocol` wire types used by the client, so collapsing them is a protocol change, not a daemon change. The 1:1 mapping is now documented and asserted instead. | done |
| A9 | L | `mult-server.rs:2152,2688` | Down from 4 copies of every PTY byte to 3 (the per-client clone and client-vec clone went away with the lease model). `Arc<[u8]>` in the broadcast path is the remainder. **Landed as far as the wire allows:** history chunks, live frames and replay frames are now one shared `Arc<[u8]>`, so *resident* copies dropped from 2 to 1. The count stays at 3 because `PtyOutput`/`ReplayChunk` carry `Vec<u8>`; removing the last copy is a protocol type change and belongs to F8's bump. **Closed fully (R10a):** F8's bump moved both to `Arc<[u8]>` (serde's `rc` feature — a feature of a dependency already present, not a new one), so the wire message is built by refcount bump and **no copy of a PTY byte remains anywhere on the daemon's broadcast path**. | done |
| A12 | L | `mult-server.rs:50` | `RAW_HISTORY_MAX_BYTES = MAX_MESSAGE_BYTES * 2` = 32 MiB resident per pane, unrelated to actual scrollback need. | done |
| A13 | M | `pty.rs`; `protocol/lib.rs` | A 1-row or 1-column pane panics `fnug-vt100` ("attempt to subtract with overflow") on a stray non-UTF-8 byte or an emoji. **Upstream defect** in `fnug-vt100` 0.15.2 (`grid.rs` subtracts from a dimension unchecked) — keep this note across a dependency bump. Floor established empirically at **2×2**: every smaller size panics on at least one ordinary byte class, and 2×2 and up survived the hand corpus plus 12 000 seeded streams. `MIN_SCREEN_ROWS`/`MIN_SCREEN_COLS` now live in `bounded_screen_dimensions`, so the client, the daemon and the wire share one answer and no `.max(1)` call site can undercut it; `PtyDimensions::clamped` (the fields stay `pub`, so no private constructor) is applied on every route into a parser and every size put on the wire. A pane below the floor renders a "too small" marker instead of a wrongly sized screen. | done |
| A14 | M | `pty.rs` | Second `fnug-vt100` panic: narrowing a screen that holds a double-width char in the last column unwraps `None` on the next print. **Upstream defect** in `fnug-vt100` 0.15.2 — `Row::resize` truncates without clearing the orphaned `is_wide` flag, unlike its own `Row::truncate`; keep this note across a dependency bump. Repro: 8 columns, wide glyph at columns 6–7, narrow to 7, print at the last column → `screen.rs:928` unwraps `None`. **Decision: worked around in our code.** `repair_wide_cells_before_narrowing` erases the split glyph before the resize, which destroys nothing the truncation was not already destroying, and skips entirely unless a row ends in one. Residual: `set_size` resizes the alternate grid too and only the current grid is reachable through the public API, so a pane narrowed while in the alternate screen can still carry a split glyph back to the normal grid on exit; entering the alternate screen clears it, so the gap is one-directional. | done |

**Fixed by `main`, do not re-do:** A3 (`SessionIdentity` namespacing), A4 (`TakenOver` notification),
A5 (per-pane failures return `LeaseRejected`, no connection teardown), A6 (`drive_termination`
signals both process groups, SIGTERM→SIGKILL).

## B. Client PTY / runtime

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| N4 | H | `pty.rs:1499-1537` via `drain_events` → `reconnect_or_report` | On reconnect, **every** terminal is re-attached serially, each with its own 2 s `ATTACH_ACK_TIMEOUT`, on the render thread. A dead daemon with N terminals freezes the UI for ~2N seconds *every frame it retries*. | done |
| B6 | H | `pty.rs:558,1471,1499,2348` | Blocking connect/hello/create/attach on the render thread: ~8 s worst case for one start. **Conflict:** `main` rebuilt the client as strictly synchronous request/response with idempotency keys and request caching, so only *connection establishment* can move off-thread — the correlated waits cannot, without a redesign. | done |
| B9 | M | `app.rs:1181`; `runtime.rs:236,2385`; `storage.rs:479` | No cap on `ChatMessage.text`; `save_if_dirty_with` runs every tick when dirty, doing `to_string_pretty` of the whole project + `sync_all` + rename + directory `sync()` — once per streamed delta. No rate limit. **Fixed (R3):** `MAX_CHAT_MESSAGE_BYTES = 64 KiB` (~10 000 words — far past what is readable in a chat pane, and cheap to re-serialize); overflow appends a `[truncated]` marker, drops further bytes and returns *unchanged*, so a runaway agent stops dirtying the project as well as stopping the growth. Truncation never splits a UTF-8 character. Saves are rate-limited by `SaveSchedule` to one per second, exempting quit, signals, fatal host-terminal errors, agent launches and structural changes (`App::has_structural_change`, set by every workspace/chat/terminal add/delete). Note the capped path is Phase 3.4 scaffolding with no production caller (see ROADMAP §"incomplete public paths"), so this is about being safe to wire up. | done |
| B16 | M | `runtime.rs:224,236,269,1442` vs `storage.rs:33,339` | **New.** `main` acquires a lock-holding `storage::StateStore` in `main.rs`, but `runtime.rs` still saves through the *free* `storage::save`, which re-derives the path from env at call time. Two save paths, only one holding the lock — split-brain. (This, not v1's F11, is the real defect.) **Fixed (R3):** `runtime::run` takes `&storage::StateStore` and every save in the client goes through it, including the launch/quit/fatal checkpoints, so the lock owner is the only writer by construction. The thread-local `ACTIVE_RUNTIME_STORE` that had been bridging the free function to the locked directory descriptor is deleted (it was a hidden second path, keyed by thread), and the free `storage::save` now simply acquires the same lifetime lock for its whole write — so a library caller fails with `WouldBlock` while a session owns the state file instead of racing it. The lock is untouched as a lock: **no trait, no `FileStateStore`/`MemoryStateStore`**, per F11. | done |
| B3 | M | `runtime.rs:236,2122,2203` | The `ensure_private_dir` storm is gone, but the status journal is still polled every ~16 ms tick per chat with a live agent: `open(O_NOFOLLOW)`+`fstat`+`seek`+`read_to_end`+`close`. Needs a ≥250 ms timer. **Fixed (R3) with S3:** `AgentStatusBridgeState` now polls on a 250 ms timer and caches one `AgentStatusSource` per chat (path + cursor), rebuilt only when the identity/generation changes and dropped when the chat stops — so the four allocations and five syscalls per chat per tick became the same work at 4 Hz, a 16× reduction. | done |
| B5 | M | `pty.rs:665,901,1242,1632,1671` | Overwrite-without-shutdown is gone, but every `self.connection = None` drops the socket without `shutdown(Both)`, so the old reader thread stays parked on a live fd. Thread/fd accumulation across reconnects persists. `ServerConnection.writer` is a `UnixStream` clone, so the fix is available. | done |
| B7 | M | `pty.rs:893-910` | `drain_events` loops `try_recv` until `Empty` with no byte or message budget. | done |
| B8 | M | `pty.rs:1392-1398` | `ServerMessage::Error` still attributed via `pane_to_terminal.values().next()` or `Terminal(0)`. **Changed fix:** the protocol now documents `Error` as connection-wide and per-pane failures use `LeaseRejected`, so adding a `pane` field is wrong — route to a status surface instead (needs E2). **Fixed (R7b):** a new `PtyEvent::ConnectionError { message }` carries no pane, `ServerMessage::Error` and every connect/handshake failure raise it, and the render loop pushes it onto E2's status surface. No wire change; `PROTOCOL_VERSION` stays 10. | done |
| B11 | M | `runtime.rs:244,250,252` | Save-failure half is fixed (`record_save_failure` + `cancel_quit`). Still fatal: `terminal.draw(...)?`, `event::poll(...)?`, `event::read()?` propagate out of `run` and kill the session, skipping cleanup. | done |
| B4 | M | `agent.rs:144-146,177,181` | `send_event` still blocking `SyncSender::send` from the render thread. (Low practical impact while F1's path is unwired, but it is a live trap for Phase 3.4.) | todo |
| B10 | M | `agent.rs:303` | `from_utf8_lossy` on raw 8192-byte read boundaries; no carry of a trailing partial sequence. Same caveat as B4. | todo |
| B15 | L | `protocol/lib.rs:734` | `vec![0; len]` commits up to 16 MiB before any payload byte arrives. | todo |
| B12 | L | `pty.rs:840-842` | `scroll_up` does `rows as i32` unsaturated; `scroll_down` clamps correctly. | todo |
| B13 | L | `agent.rs:252-257` | `Drop` kills without `wait()`; zombies. | todo |

**Fixed by `main`:** B1 (`key_for_pane` returns `Option`, no parser synthesis), B2 (generation-stamped
status journal, daemon authoritative, no `Done`→`Thinking` resurrection). **Obsolete:** B14
(`next_durable_candidate` replaced by a `BTreeSet` search that cannot wrap).

## C. Security

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| C3 | H | `mult-server.rs:702-745`; `pty.rs:2213-2256` | Unchanged from v1: `peer_uid` returns `Ok(None)` off Linux and both callers treat it as **accept**. Squatted-socket keystroke capture on macOS/BSD still holds. Needs `getpeereid`, fail-closed, and one shared implementation (F7). | done |
| C2 | H | `config.rs:126,140` | `load_from_path` is still a bare `fs::read` — symlink-following, unbounded, no owner/mode/regular-file check — and its `pi_agent_command`/`claude_code_command` go to `$SHELL -lc` and auto-start by default. `main` hardened *state* reads but not *config*. Use the existing `SecureDirectory` / `validate_private_regular_file`. | done |
| S2 | M | `transcript.rs:190-193`; `storage.rs:640-642` | **New.** `TranscriptJournal::open` has two destructive side effects on a caller-supplied path: `read_and_recover` calls `file.set_len()` on any file whose tail lacks a newline, and `open_parent(.., normalize_parent: true)` **`fchmod`s the parent directory to 0700**. A mistyped path silently truncates a file and changes a directory's mode. Unwired today (S1), so fix before Phase 3.4 wires it. | done |
| C6 | M | `storage.rs:251` | State read is now `openat(O_NOFOLLOW)` + `validate_private_regular_file` + `fchmod 0600` — but `read_to_end` has **no size cap**, so a large regular state file still OOMs. Port only the cap. | done |
| C7 | M | `git.rs:6-26`, called `runtime.rs:299` | Still `Command::new("git").arg("-C").arg(cwd)` every 2 s per workspace, with no `GIT_CONFIG_NOSYSTEM` / ceiling / `--git-dir` pin — so a hostile repo's `.git/config` (`include.path`, `core.fsmonitor`) is parsed merely by opening the workspace. v1's `.git/HEAD` reader ports, but into `git::current_branch` directly (no `BranchProbe` trait here). | done |
| C8 | M | `pty.rs:2313-2329,2362-2379` | `server_executable` resolves by filename next to `current_exe()` with no owner/mode check; `spawn_server` has no `env_clear()`, so the long-lived daemon inherits the first client's full environment (API keys) and passes it to every later client's PTYs. | done |
| C9 | M | `pty.rs:432-457,1950-1977` | Terminal query responses accumulate uncapped and are emitted one socket write each, on the render thread — ~2048 writes per 8 KiB of `\x1b[6n`. | done |
| C13 | L | `runtime.rs:553,561,647-655` | OSC 52 still written straight to `io::stdout()` outside the ratatui frame on every mouse-up, no opt-out, no tmux passthrough. | done |
| C14 | L | `pty.rs:460-463` | `append_terminal_system_line` feeds server-supplied text (`PtyEvent::Error`, `ExitInfo::signal`) to `parser.process()` with no control-byte stripping — UI spoofing within the emulator. | done |
| S6 | L | `storage.rs:807-829` | **New.** `validate_private_regular_file` checks regular/owner/`nlink` but not `mode & 0o077`, while its sibling `read_mult_agent_status_records` (`runtime.rs:2222`) does. One-syscall window (it `fchmod`s right after), but close the inconsistency. | done |
| S7 | L | `paths.rs:38-40` | **New.** `resolve_user_base` accepts any *absolute* `$XDG_CONFIG_HOME`/`$XDG_DATA_HOME` with no ownership check. Harmless for state (re-validated by `SecureDirectory`) but it makes the config path attacker-steerable via a second env var. Closed by C2. | done |
| S8 | L | `mult-claude-status.sh:36`; `mult-status.ts:33-48` | **New.** The C5 remediation dropped `O_NOFOLLOW` discipline along with the temp file: `[ -f ]`/`statSync` follow symlinks and append through them. Only reachable inside a directory already certified 0700-and-owned, so low. | done |

**Fixed by `main`:** C1 (never relaunches persisted `Command`; better than v1's), C4 (status path
fails closed), C10 (content-addressed `-v2` runtime artifacts + rotation + removal on exit),
C12 (`SessionIdentity` capability gate). **Obsolete:** C5, C11 (the predictable-temp-file and
0755-mkdir schemes no longer exist; the extensions now append to a file `mult` pre-creates).

## D. Performance

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| D1 | H | `runtime.rs:1823-1853`; `pty.rs:867-891` | **Worse than v1:** `changed` is computed for the return value only, and `resize` is called unconditionally — now from **two** sites (terminal *and* chat agent), so a `Resize` is serialized and written to the socket 125×/s at idle. One-line gate at both. **Fixed (R3):** both sites go through `resize_if_changed`, which calls `PtyRuntime::resize` only when the parser's dimensions differ; a pane with no parser yet still resizes once (that call creates the parser, so the next tick is a no-op). Regression test drives a recording fake daemon and asserts no size is written twice across eight idle ticks. | done |
| S3 | M | `runtime.rs:2141-2148` | **New, and now the largest idle cost.** The status bridge runs every 16 ms tick per agent chat: `format!` + 2 `join`s + a `PathBuf` clone (4 allocations) then `open`+`fstat`+`seek`+`read`+`close`. With 4 chats ≈ 1250 syscalls/s and ~1000 allocations/s at idle. (Same root as B3.) | done |
| D2 | H | `ui.rs:784,805-889` | `TerminalScreen::from_vt100` deep-copies every cell per frame into `Vec<TerminalCell>` with `symbol: String`, and `ui.rs:881` calls `.to_string()` on `contents()`, which already returns an owned `String`. Blank cells still call `contents()`. v1 measured 414 µs / 20 000 allocs per frame. **Fixed (R6):** redundant clone dropped, blank cells short-circuited, and the symbol stored inline (`CellSymbol`, 24 bytes = vt100's own six-codepoint cell maximum) with a heap arm as a safety net. Measured at 200x50: 755 -> 578 µs and 15 189 -> 2 585 allocations per frame, rendered buffer byte-identical. | done |
| D3 | H | `ui.rs:708-711`; `app.rs:1082,1139` | `terminal_all_lines` (a `String` per row) is an eager argument scraped every frame, but the callee returns `None` immediately when no search is active. **Fixed (R6):** `terminal_search_matches` now takes a closure. The chat half (`filtered_chat_lines`, `app.rs:1139`) was re-checked against current code and is *already* lazy — it scrapes after its early return — so only the terminal path needed changing. The removed scrape measured 42-46 µs and 2 696 allocations per frame. | done |
| D4 | M | `runtime.rs:44-46,219-271` | `main` added `needs_redraw` gating (real improvement), but the 16 ms tick still runs D1 ×2, S3, both drains, `save_if_dirty_with` and two workspace scans. The wakeup-source half is still open. **Re-measured after R3** (the tick body itself, not the wakeup): D1 ×2 now cost two `HashMap` lookups and a size comparison and write nothing; S3 polls at 4 Hz instead of 62.5 Hz; the save is at most 1 Hz; the git scan is at most 0.5 Hz and no longer forces a frame. What remains at complete idle is ~62 wakeups/s, each doing two `try_recv`s on empty channels, two selected-pane layout computations for the resize checks, and one `Instant::now()` — no syscalls beyond the `poll` itself, no allocations, no socket traffic. The wakeup-source rework is therefore now about the ~62 timer wakeups, not about the work inside them. | todo |
| S4 | L | `app.rs:326,338`; `runtime.rs:228-230` | **New.** `replace_workspace_git_branches` returns `bool`, but the caller discards it and sets `needs_redraw = true` unconditionally — defeating `main`'s own redraw gating twice a minute at idle. **Fixed (R3):** `refresh_workspace_git_branches` forwards the return value and the loop does `needs_redraw \|= …`. Tested against a hand-built `.git/HEAD` fixture: first probe true, unchanged probe false, branch switch true. | done |
| D5 | M | `git.rs:11`; `runtime.rs:227,293` | `git` forked synchronously on the UI thread every 2 s per workspace. Mostly resolved by C7's `.git/HEAD` reader. **Verified and closed (R3):** re-read against current code — `git.rs` no longer references `Command` at all; `current_branch` is an `O_NOFOLLOW`, `O_NONBLOCK`, size-capped read of `.git/HEAD` after a bounded (64-deep) ancestor walk. The residual cost is a handful of `stat`/`open`/`read` syscalls per workspace every 2 s on the UI thread, which is not worth a thread; S4 removed the redraw it also forced. No further code change was needed. | done |
| D6 | M | `pty.rs` | `TerminalResponseState::Csi(Vec<u8>)` heap-allocated on entering every CSI sequence. Replaced by an inline `[u8; TERMINAL_MAX_CSI_SEQUENCE_BYTES]` plus a length held on the detector across states, so entering a CSI allocates nothing. | done |
| D7 | M | `pty.rs` | Every escape-sequence byte took `parser.process(from_ref(&byte))`. The detector's state machine is now screen-free (`TerminalQuery` resolves against the screen separately), so `feed_parser_with_responder` scans one whole escape sequence ahead and feeds it in a single `process` call. The CPR carve-out is preserved — batching stops at every sequence boundary, and the only answering sequences never move the cursor — and the doc comment now describes the batching rather than contradicting it. G4 pins it. | done |
| S5 | L | `runtime.rs:1725` | **New.** `write_private_runtime_file` takes a *blocking* `flock(LOCK_EX)` on a fixed path from the UI thread; a second `mult` starting an agent stalls the first one's render loop. Use `LOCK_EX\|LOCK_NB` + retry. **Fixed (R3):** a `RuntimeFileLock` guard takes `LOCK_EX\|LOCK_NB` with 5 attempts 2 ms apart (10 ms worst case, versus unbounded) and unlocks on drop; `EINTR` retries, any other error gives up at once. Losing the race degrades to launching the agent without its status extension — the fallback the caller already had for an unwritable artifact. | done |
| D8 | L | `ui.rs:464-489` | `truncate_text` does `ch.to_string()` + `Span::raw` per character. **Fixed (R6):** a new `char_width` encodes into a stack buffer and reuses the existing `text_width`; `Span::raw` borrows a `&str`, so it never allocated — only `to_string` did. No `unicode-width` dependency added (it is a transitive dep of ratatui/vt100, not a direct one). | done |
| D9 | L | `ui.rs:63-78,153` | `Palette::from_colorscheme` re-parses 12 hex strings every frame; no memo. **Fixed (R6):** memoized in a `OnceLock` sited on `ColorSchemeConfig` itself, so the cache travels with the strings it was derived from and no scheme can inherit another's palette (`default`, deserialize and `clone` each start with an empty cell). This also closes the palette half of F20: `DEFAULT_COLOR_SCHEME` in `config.rs` is now the single source and `ui`'s `moon::*` constants are `const fn`-derived from those same literals, so divergence is a build error rather than a silent drift. `Palette::from_colorscheme_reporting` returns per-key parse failures (`ColorParseIssue`) as the seam R7/E6 will surface as startup warnings. | done |
| D10 | L | `pty.rs:48-52,1103,1350`; `runtime.rs:1885` | `PtyEvent::Output`/`Scrollback` carry full payloads through the queue to an empty match arm. **Fixed (R6):** both now carry `byte_count`; the bytes are already committed to the terminal's parser before the event is queued, so nothing was reading them. `tests/pty_integration.rs` matches only on `terminal`, so it was unaffected. | done |
| D11 | L | `pty.rs:142` | `SERVER_EVENT_QUEUE_CAPACITY = 4096` × 8 KiB ≈ 32 MiB client-side backlog if the UI thread stalls. **Fixed (R6):** 4096 -> 256 (~2 MiB; overflow parks the reader thread, applying backpressure to the daemon instead of buffering for it, and the synchronous request loops drain the queue while they wait), and adjacent same-pane `Output` events are coalesced on drain so a burst costs one event rather than one per chunk. | done |

## E. UX, CLI & configuration

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| E1 | H | `main.rs:18`; `mult-server.rs:206` | No argv handling in either binary; no `std::env::args`, no `default-run`. `mult --version` launches the TUI. **Fixed (R7a):** new `src/cli.rs` (hand-rolled; no `clap`, per the no-new-dependencies rule) parses both grammars through one pure `parse(Binary, args)` — `--help`/`-h`, `--version`/`-V` from `CARGO_PKG_VERSION`, `--config`/`--state`/`--socket` in both `--flag value` and `--flag=value` form, values split on bytes so a non-UTF-8 path survives. An unknown flag, a stray positional, a missing or repeated value all exit 2 with a message and a `--help` pointer; `mult-server` **rejects** `--config`/`--state` instead of accepting a no-op. Precedence is flag > env > default, implemented through R11's pure seams (`config_path_from` gained a flag argument, `StatePaths::resolve_with`, and `default_socket_path` consults a set-once `OnceLock` the CLI installs — no `set_var`). `default-run = "mult"` added. 12 tests. | done |
| E2 | H | `pty.rs:213,1398,1489` | Daemon-connection failure still queued at `PtyKey::Terminal(TerminalId(0))`, which cannot exist. No status/notice surface in `app.rs`. Blocks B8. **Fixed (R7b):** `App` gained a dismissible status surface — `Notice { level, source, text, expires_at }`, `push_notice`/`push_notice_at` (clock injected, so the TTL tests are deterministic), `NOTICE_TTL` 12 s, `MAX_NOTICES` 4 oldest-dropped, and de-duplication that refreshes a repeating sentence instead of stacking copies of it. It occupies rows only while it has something to say, so an untroubled session gives every row back to the panes. `Ctrl+n` and a "Dismiss notices" palette entry clear it; `Ctrl+n` is only consumed when there is something to dismiss, so it still reaches a PTY otherwise. Routed into it: `PtyEvent::ConnectionError` (B8, replacing the `Terminal(0)` attribution), save failures (sticky via `NoticeSource::SaveFailure`, retracted by a successful save), the config loader's warnings (E6) and `LoadedState::notice` (E11, pushed once by `main` as `NoticeSource::Report`). | done |
| E5 | H | `config.rs:141,219`; `main.rs:30` | Bad config still `Debug`-dumps with no filename. `main` returns `io::Result`, so v1's `ExitCode` fix needs adapting. **Fixed (R7a):** both binaries return `ExitCode` and print `Display`, never `Debug`. Parse failures read `config error at <path>:<line>:<col>: <message>`, with serde's redundant trailing " at line N column M" stripped. R4's `O_NOFOLLOW` symlink rejection — which failed *inside* `openat`, so the user saw a bare `Os { code: 40, kind: FilesystemLoop, … }` naming nothing — is mapped by `ELOOP`/`ENOTDIR` to a message naming the path, saying symlinked configs are unsupported, and giving the workaround. The shared reader's directory messages are parameterized by `DirectoryPurpose`, so a config parent failure now says **config**, not "state parent"/"private state directory". Config is also loaded *before* the state lock is taken, so a broken config reports itself rather than whatever the state path had to say. | done |
| E4 | H | `ui.rs:1732` | No in-app help; `keybinding_help_line_is_not_rendered` still asserts the footer's absence; no shared binding table. **Fixed (R7b):** one `app::BINDINGS` table is now the single source for both the command palette and a new `?`/`F1` overlay — a row with an `action` is offered by the palette, a row with `keys` is listed by the overlay, and most have both, so neither can drift from the other. `?` is gated on there being no PTY input target (a selected pane takes every ordinary key, so a global `?` would steal a character from every shell and pager); `F1` and the palette's "Show keybindings" are unconditional, and any key closes the overlay. The overlay is drawn over the frame rather than in the layout, so it costs nothing while down. `keybinding_help_line_is_not_rendered` is kept as-is and is still correct: the old always-on footer stayed deleted; the overlay replaced it. **Finishing pass:** the overlay was capped at 64 columns whatever the terminal was, so its longest labels were clipped mid-word by the renderer with nothing marking the cut, and being borderless it ran into the pane underneath ("▣ websKeybindings"). It is now a bordered panel sized to its widest row, with `truncate_text` ellipsizing any label that still will not fit. Pinned by `snapshot_help_overlay` and `the_help_overlay_fits_its_labels_and_marks_the_ones_it_cannot`. | done |
| E6 | M | `config.rs:13-31` | No `deny_unknown_fields`, no colour-parse reporting, no project-path validation. **Fixed (R7a):** `deny_unknown_fields` on `Config`, on `ColorSchemeConfig`, and — via a hand-written visitor replacing the `untagged` enum, which reported every failure as "data did not match any variant" and lost the position — on a `projects` object entry. Policy, written into `docs/CONFIG.md`: anything that means the file cannot be understood (bad JSON, wrong type, unknown key, a failed ownership check) is a **startup error**; anything with a safe fallback is a **warning** — a colour that will not parse keeps the built-in default (through R6's `Palette::from_colorscheme_reporting` seam) and a `projects` entry whose expanded path is not a directory is still offered. Warnings are collected at load and exposed as `Config::warnings() -> &[String]`. | done |
| E11 | M | `storage.rs:56,185,255`; `model.rs` | `main`'s backup mechanics are better than v1's (`SecureDirectory`, `.corrupt-*`, 0600, V1→V2 migration) — but `LoadedState` carries **no user notice**, so the user is still never told, and only 4 `#[serde(default)]` exist, so one renamed/`null` required field still discards every workspace. Port the lenient-decode + notice halves only. **Note (R11):** the current behaviour is now pinned by `a_shape_error_backs_the_file_up_and_resets_to_a_fresh_state` and `a_failed_backup_rename_aborts_the_reset_and_keeps_the_original_bytes` (G5); E11 changes the policy and must update both. **Fixed (R7a):** decoding is now lenient about fields that can be *reconstructed* — the ID allocator hints, the identity and generation tables, a workspace's `cwd`/`environment`/`chats`/`terminals`, a session's `status`/`launch`/`agent`/`messages` — via `#[serde(default, deserialize_with = "null_or_default")]`, which covers a missing field *and* an explicit `null`. IDs, names and the namespace stay required, and a wrong *type* is still an error rather than data silently dropped. `ProjectState::normalize_session_identities` reconciles the identity and generation tables with whatever decoded (stale entries dropped, duplicate tokens re-minted, missing ones minted fresh — fail-safe, since a fresh token cannot claim an existing daemon session), so a partially decoded file loads instead of failing `validate_session_identities`. When a reset is genuinely unavoidable, `LoadedState::notice` carries a sentence naming the file, the decode error and the backup path; `main` prints it and pushes it into the app notice surface (R7b). `a_shape_error_backs_the_file_up_and_resets_to_a_fresh_state` became `a_shape_error_backs_the_file_up_and_reports_where_the_backup_went`; the rename-failure test is unchanged. New `a_partially_unknown_state_keeps_everything_it_can_decode` and `leniency_does_not_weaken_the_version_boundary` (future version still rejected byte-for-byte, V1 still migrates). | done |
| E7 | M | `app.rs:1375-1412` | Prompt input still append-only across four duplicated arms; no cursor. **Fixed (R7b):** a `PromptInput { text, cursor }` owns the text for every prompt, with the cursor counted in *characters* and byte offsets derived on demand, so no edit can land on a non-char boundary. `PromptEdit` covers insert, `Backspace`, `Delete`, `Left`/`Right`, `Home`/`End`, `Ctrl+a`/`Ctrl+e`, `Ctrl+w` and `Ctrl+u`, applied in one place. The renderer emits the text in up to three spans — before the cursor, the character under it, after it — so concatenating them reproduces the string exactly and a wide or combining character keeps its own display column instead of being replaced by a block; the only synthetic span is the block drawn past the last character. | done |
| E8 | M | `ui.rs:310` | Chat state carried by colour alone (`"● "` for every state). **Fixed (R7b):** six single-width, long-standing Unicode glyphs (no Nerd Font, no emoji font) carry the state and colour reinforces it — `◐` thinking, `?` waiting on the user, `✗` failed, `✓` finished-and-unseen, `●` finished-and-seen, `○` idle. The sidebar's selected-row highlight deliberately sets a background (or reverse video) and no foreground, so selecting a row cannot put its state back on colour alone. | done |
| E12 | M | `README.md:147`; `docs/DAEMON.md:147`; `ui.rs:610` | **New (F1 residual).** `MULT_AGENT_CMD` is documented as a live knob but is inert, and chat search silently searches an always-empty transcript. Mark experimental/no-op — do **not** remove the code. **Fixed (R12 + R7b):** R12 already marked `MULT_AGENT_CMD` **experimental and inert** in the README env table. The chat-search half is done now: `Ctrl+s` on a chat searches the *structured* transcript, which only the unwired process-agent backend writes, so it is empty for every chat a user can create and "No matches." read as "your text is not in this chat". The chat pane now says the transcript is not populated and that chats run in a PTY; the README marks chat search as not wired up and the `Ctrl+s` row points at the note. Nothing was deleted — `src/agent.rs`, `ChatMessage`, `ChatMessageRole` and `ProjectState::messages` all stay, because `src/transcript.rs` builds on them. | done |
| E9 | L | `app.rs:158-175`; `main.rs:35` | No config reload; `config` moved into `runtime::run`. **Fixed (R7b):** a "Reload config" palette entry sets a flag the event loop takes, because the palette handler holds only `&Config` while the loop owns it. On success the new warnings go to the status surface and a notice states what did *not* change — `mouse_capture` is a terminal mode set once per session, and a running PTY keeps the command it started with — so an applied reload never looks like a no-op. A failed reload keeps the previous config and says so, rather than ending a session holding live PTYs over a colorscheme typo. `--config` is deliberately not re-resolved (it is `main`'s, and threading it through the loop for this is not worth it); `$MULT_CONFIG_PATH` and the default path still apply. | done |
| E10 | L | `ui.rs` | `NO_COLOR` appears nowhere in the repo. **Fixed (R7b):** read once into a `OnceLock` (it is a process global, and G7 forbids a test mutating one), selecting `Palette::monochrome()` where every colour is `Color::Reset`. Emphasis that would have been a background colour becomes reverse video, which every terminal has, and E8's glyphs mean status still reads with no colour at all. Pinned by `snapshot_no_color_frame`, whose style legend is the assertion, plus `no_color_emits_no_color_at_all_and_keeps_overlays_distinguishable`. | done |

**Fixed by `main`:** E3 (`Prompt::ConfirmDelete` — and `main` always prompts, with no empty-item
skip, so it is strictly safer than v1's).

## F. Architecture

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| F1 | — | — | **ABANDONED.** See "Decisions recorded". Residual is E12 only. | dropped |
| F2 | H | `pty.rs:133-145` | **Worse than v1:** `PtyRuntime` now has **12** parallel maps keyed by `PtyKey`/`PaneId` (was 8) — `pane_leases`, `expected_output`, `session_identities`, `agent_sessions` were added. Every lifecycle op must touch the right subset. **Done (R9):** the twelve are one `PtyPane` in `panes: HashMap<PtyKey, PtyPane>`, plus `pane_index: HashMap<PaneId, PtyKey>` maintained only by `bind_pane`/`clear_attachment`. `remove_terminal` is now one `panes.remove`, so nothing can be forgotten. Every lifecycle path was checked field by field (`remove_terminal`, `reset_parser`, `stop`, `start`/`start_attached`, `attach_existing`, `perform_attach`'s reconnect and replay-end, `PaneExited`, `TakenOver`, `LeaseRejected`, sequence-gap, `install_connection`, `Drop`); two regression tests pin the retire-everything and clear-only-the-attachment boundaries. | done |
| F7 | H | `pty.rs:2214-2263` vs `mult-server.rs:706-748` | **Worse than v1:** peer check still byte-duplicated, and `current_euid` now exists in **three** places plus three inline `libc::geteuid()` and one in protocol. No `crates/protocol/src/peer.rs`. Blocks C3. | done |
| F9 | M | `main.rs:13`; `lib.rs` | `runtime` is still binary-private and 3904 lines, fusing event loop + keymap + mouse + clipboard + hook generation + status polling + git + save. Tests cannot reach it. **Done (R10b).** `pub mod runtime;` in `src/lib.rs`; `src/runtime.rs` (5428 lines) is now `src/runtime/` — `mod.rs` (the loop, per-tick ordering, config reload, host-terminal failure policy), `input.rs`, `prompt.rs`, `keymap.rs`, `mouse.rs`, `clipboard.rs`, `session.rs`, `agent_launch.rs`, `agent_command.rs`, `agent_status.rs`, `save.rs`, plus a `#[cfg(test)] test_support.rs` holding the scripted-daemon and state-store fixtures once instead of once per split module. The 68 runtime tests moved from the `mult` binary's test target to the library's, where `tests/` and the rest of the crate can reach them; only `terminal_guard` stays binary-private. Largest resulting file: `agent_status.rs` at 812 lines. | done |
| F15 | M | `ui.rs` (2272 lines) | Contrast maths + vt100 adapter + all widgets still fused. **Done (R10b).** `src/ui.rs` (3504 lines) is now `src/ui/` — `theme.rs` (palette, colour parsing, WCAG contrast, `NO_COLOR`), `vt_screen.rs` (the `vt100` → `tui_term` adapter with R6's inline `CellSymbol`), `terminal_view.rs` (drawing a live screen plus the selection highlight), `sidebar.rs`, `main_pane.rs`, `prompt.rs`, `status.rs` (the notice surface, lifted out of `draw_prompt_area` as `draw_notice_surface`), `help.rs`, `text.rs`, and a `test_support.rs`. `src/snapshots/` moved to `src/ui/snapshots/` with `git mv`; the snapshot names derive from the module path (`mult__ui__tests__*`), so keeping the six snapshot tests in `ui/mod.rs` left all seven files byte-identical (`git diff -M --numstat` reports `0 0` for each) and no `.snap.new` was produced. | done |
| F5 | M | `app.rs:23-24,316,605` (2614 lines) | `prompt: Option<Prompt>` and `focus: FocusMode` still orthogonal; `sync_focus_to_selection` hand-sprinkled at three sites. **Done (R10b).** One private `mode: InteractionMode` replaces both fields: `Prompting { prompt, resume }` or `Browsing { focus }`, where the focus is a `BrowseFocus` (`Sidebar` or `SelectedPane`) that does not name a pane — which pane is the selection's business, so a focus disagreeing with the selection is unrepresentable. `App::focus()` derives the renderer's `FocusMode` and returns `None` while a prompt is up, which is exactly what `ui` meant by `!is_prompt_active() && focus == …`. `resume` exists because cancelling a prompt has always returned the session to the focus it had (reachable via the palette's "Return to sidebar"); it is never observable while the prompt is open. `set_browse_focus` moves the focus without disturbing an open prompt, so a PTY exiting under the palette still re-derives where it will land without closing it — pinned by `a_pty_exiting_under_an_open_prompt_leaves_the_prompt_alone`. `App::new` starts in `Browsing`: nothing at startup opens a prompt, and a recovered command terminal's deliberate-start requirement is the per-terminal `terminal_requires_recovery` flag, not a modal surface. `?`-opens-help is unchanged — `help_key_opens_help` reads `pty_input_target()`, which is selection-derived and independent of focus. `src/app.rs` (3687 lines) is now `src/app/` — `mod.rs`, `nav.rs`, `prompt.rs`, `open_workspace.rs`, `search.rs`, `selection.rs`, `notices.rs`, `bindings.rs`, `mutate.rs`. | done |
| F6 | M | `ui.rs:152,177,182,186` | No `src/layout.rs`; `layout_areas` recomputed at three call sites per iteration; renderer is still the geometry oracle. **Done (R10b).** `src/layout.rs` holds `AppLayout { area, sidebar, main, prompt }` and `AppLayout::compute(&App, Rect)`, with `prompt_height` (which depends on the open prompt *and* the live notice count) moved out of `ui`. The loop resolves it once per iteration — after every drain that can change what the frame has to hold, and immediately before the resize/auto-start handlers and the draw — and passes it to both. `ui` only consumes rects; `selected_terminal_output_area`/`chat_agent_output_area_for` and friends are `AppLayout` methods now. Two guards: `draw_with_palette` recomputes when `layout.area != frame.area()`, because `ratatui` resizes its buffer inside `Terminal::draw` and the tick after a host-terminal resize would otherwise paint one frame at the old geometry with `needs_redraw` already cleared; and the loop re-resolves after a draw that reported a different area, so the events read below hit-test against the geometry the user is looking at, as they did when the handlers got the freshly drawn `frame_area`. `a_notice_resizes_the_visible_pane_once_without_a_terminal_resize` pins that a layout change with no terminal-size change reaches the daemon exactly once (D1's "only when the dimensions changed", now answered against one value instead of three copies). | done |
| F3 | M | `pty.rs:320-385`; `runtime.rs:273` | **Premise changed:** `impl Default for PtyRuntime` not only still exists, it is the *production* constructor — so the `SpawnPolicy` fix must also replace that call site, which v1's diff does not. **Done (R9):** the `Default` impl and the dead `PtyRuntime::new` are gone; `runtime::run` constructs `PtyRuntime::with_socket_path(default_socket_path(), SpawnPolicy::Autospawn)` explicitly, and the threaded `allow_spawn: bool` is `SpawnPolicy::{Autospawn, ConnectOnly}` through `connect_inner`, `start_connector` and `establish_connection` — so the connector thread carries the policy rather than a bare flag. | done |
| F12 | M | `model.rs:339-390`; `storage.rs:192,228` | `try_default_with` still seeds a `"website"` demo workspace, and the corrupt-recovery path still hands it to the user. **Done (R9):** `ProjectState::try_default_with` is empty and reads no environment; the seed moved to `try_first_run_with`, reached only from the branch where `read_state_bytes` found no file at all. Corrupt recovery returns the empty project plus R7a's `LoadedState::notice`, which `main` already prints and pushes to the status surface. `App::default()` (tests only — `main` builds from the loaded state) uses the first-run project so it still stands for a fresh install. | done |
| F16 | M | `model.rs:272`; `app.rs:33,636,957` | Persisted `TerminalStatus` still duelling with `PtyRuntime::is_running`; `seen_done` side table intact. **Bigger than v1:** must now coexist with `STATE_VERSION = 2`, its V1→V2 migration, and the future-version byte-preservation guarantee. **Done (R10a).** `STATE_VERSION = 3`. `TerminalStatus` is deleted from the model: `TerminalSession.restore_on_launch: bool` persists *intent*, and displayed liveness comes only from `PtyRuntime::is_running` (the one `ui.rs` site that read the persisted status now asks the runtime). `ChatStatus` gained `DoneSeen`, so `App::seen_done` is gone and the seen bit is resolved inside `mark_chat_status_by_id` — callers pass the plain status they observed and cannot desynchronize the two. Migrations chain 1 → 2 → 3; `migrate_v2_to_v3` maps `Running`/`Stopped` to `true`/`false`, keeps the existing identity table verbatim (`normalize_session_identities`, which subsumed the now-deleted `assign_session_identities`), and applies the unowned-`Thinking`/`Waiting` reset every load already did. **C1 is untouched:** restore intent grants attach, never relaunch; `recoverable_terminals` is now built from `!restore_on_launch && Command`, and the no-execution tests still pass. The future-version guarantee is unchanged and re-pinned at version 4. Round-trip coverage: three golden migrations (v1-original, v1-current, v2-current → v3) plus a load test asserting a real v2 file keeps every workspace, chat, message, command string, namespace and token. | done |
| F8 | M | `protocol/lib.rs`; `mult-server.rs:3545` | No `RejectCode`; `io::Error` still universal. The `"already attached"` match is gone, but text matching remains (`"lease space exhausted"`). Must land in the same wire bump as any other protocol change. **Done (R10a) — this slice spent the effort's single `PROTOCOL_VERSION` bump, 10 → 11.** `RejectCode` (17 variants) is on the wire; every `*Error::Failed` carries one, `ServerMessage::Error` carries one, and `CreateError`/`AttachError`/`StopError`/`AgentStatusError`/`LeaseRejectionReason` each have a total `code()`, so the whole error space classifies. `LeaseRejectionReason::WriteRefused` is R1's requested dedicated code for a full write queue, and the client now *keeps* its attachment on one instead of tearing it down. Client-side `PtyError` and `StateError` are hand-written (`Display` + `Error`, no `thiserror`); `io::Result` remains only at socket/file/framing boundaries. **No string matching for control flow remains** anywhere — the surviving `contains` calls are test assertions on rendered text. Also in the bump: `ClientMessage::{Scroll, ScrollToTop, ScrollToBottom}` deleted (F17's leftover), and `PtyOutput`/`ReplayChunk` moved to `Arc<[u8]>`, closing A9. | done |
| F10 | M | `runtime.rs:63` | No `BranchProbe` / `AgentStatusSource` seams; the new `AgentStatusBridgeState` is concrete and file-backed, so its tests hit the real filesystem. **Done (R10a).** `trait AgentStatusSource { poll, retain }` with `JournalStatusSource` (file-backed) and an in-memory `FakeAgentStatusSource` double; `AgentStatusBridge<S>` carries the poll clock and `drain_mult_agent_status_events` is generic over `S`. The timing test and a new retain/generation test run entirely on the double. The file-backed reader keeps its filesystem tests deliberately — symlink refusal, mode and size caps are a security boundary a double would not exercise. **`BranchProbe` is moot, confirmed:** R4 removed the `git` fork; `git.rs` reads `.git/HEAD` (and `packed-refs`) directly with no process, no network and a byte cap, and `refresh_workspace_git_branches` is already timed by `GIT_BRANCH_REFRESH_INTERVAL`. **No other per-frame external dependency lacks a seam:** the remaining ones are the daemon socket (already behind `PtyRuntime`, and tests drive it through a scripted `UnixStream` pair), the state store (behind `StateStore`, injectable via `StatePaths` and `IdentitySource`), the agent backend (`trait AgentBackend`, with `NoopAgentBackend`), and the host terminal (`TerminalGuard`). | done |
| F13 | M | `app.rs:582,744,766,1259` | Four copies of the wrap-around list-selection body; no shared prompt-key handler. **Fixed (R7b):** one `ListSelection` (`index`/`reset`/`clamp`/`step`) replaces the four copies, and one `classify_prompt_key` classifier feeds every text prompt through the single `PromptInput::apply`, so a motion or kill added once is present in all of them. `prompt_motions_and_kills_work_in_every_text_prompt` drives each prompt through its own handler exactly as `handle_key` dispatches it, so the sharing is asserted rather than assumed. | done |
| F14 | M | `ui.rs:459,537`; `app.rs:1437` | `sidebar_selected_index` still re-walks the order `nav_iter` claims to own. **Done (R9):** `App::sidebar_row_iter` is the one walk, emitting `SidebarRow::{Spacer, Workspace, Nav}`; `nav_iter` is its selectable rows and `App::sidebar_rows` its collected form. `ui` renders those rows one `ListItem` each and finds the highlight by position in them. `sidebar_selected_index` is deleted. | done |
| F17 | L | various | Most of the dead-surface list stands (`ensure_parser`, `scroll_to_top/bottom`, `search_status`, `focus_next`, `pane_inner`, `output_area_after_header` with both header constants `0`, and 4 unused `ClientMessage` variants). **Exceptions: `terminal_all_lines` is now live** (`ui.rs:710`), and `SessionInfo`/`PaneInfo`/`Sessions` are live. **Done (R9), with exceptions.** Deleted: `scroll_to_top`/`scroll_to_bottom`, `App::search_status`, the `#[cfg(test)]` `focus_next`/`focus_previous`/`cycle_focus`/`available_focus_modes`, and `pane_inner`/`output_area_after_header` with both zero header constants (`*_output_area_for` now returns `layout.main`). **Not dead, so kept:** `terminal_all_lines` (`ui.rs`), `SessionInfo`/`PaneInfo`/`Sessions`, `ClientMessage::Paste` (live in `send_paste`, the daemon and `pty_integration`), and `ensure_parser`, which the `fuzzing` entry point and ~30 tests call — it is now `#[cfg(any(test, feature = "fuzzing"))]` instead. **Deliberately not done:** `ClientMessage::{Scroll, ScrollToTop, ScrollToBottom}` are genuinely dead but trimming them is a wire change, and R9 is behaviour-preserving; they are left for F8/R10 to remove inside the effort's single `PROTOCOL_VERSION` bump. | done |
| F18 | L | `ui.rs:820-840` | Every agent kind still renders "Pi agent not started" and the wrong config keys. **Done (R9):** both the "not started" and the "waiting for output" lines use `AgentKind::display_name()`, and a new `AgentKind::config_keys()` names the right `*_command`/`auto_start_*` pair. `docs/TROUBLESHOOTING.md`'s "known cosmetic bug" note is retired. | done |
| F20 | L | various | `crates/protocol/src/` is `lib.rs` only. `invalid_data` ×4, `shell_command_args`+`default_shell` ×2, `shell_quote` vs `shell_display_arg`. **The palette half regressed** — `Color::Rgb` constants are back in `ui.rs:34-43` with no `DEFAULT_COLOR_SCHEME`. `random_u64` is now single-source (that sub-item is fixed). **Done (R9):** the palette half was re-fixed by R6. `invalid_data` is `mult_protocol::invalid_data` (it was ×3, not ×4 — `config.rs` has no copy); `default_shell` and `shell_command_args` are `mult_protocol::shell`. `shell_quote` and `shell_display_arg` are **not** the same function — different safe-character sets (`+` vs `=`) and different empty-string handling — so they are `shell::quote_argument` (executed) and `shell::quote_display_argument` (shown) over a shared `single_quote` core, with a test pinning the three inputs they disagree on. | done |
| F19 | L | `app.rs:1315,1425,1717,1968` | **Scope reduced:** the full `terminal`→`pty` rename now conflicts with ~8k lines of rewritten code for little gain. Reduce to deleting the three identical alias pairs, which is conflict-free. **Done (R9):** `end_terminal_input`, `normalize_focus` and `selected_output_terminal_id` are deleted; `end_pty_input`, `sync_focus_to_selection` and `pty_input_target` are the survivors, with the body moved into `pty_input_target`. The rename was not attempted. | done |
| F21 | L | `app.rs:744,766` | Non-modular wrap in the list-step body, twice. Fold into F13. **Fixed (R7b, with F13):** `ListSelection::step` uses `rem_euclid` in both directions. The old `index.checked_sub(delta).unwrap_or(len - delta)` only happened to be right for `delta == 1` and underflowed for `delta > len`. | done |

## G. Tests

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| S1 | M | `transcript.rs`; `lib.rs:9` | **New.** `TranscriptJournal` has zero call sites outside its own tests — 344 lines of file-truncating I/O shipped in the binary, exercised only by tests. Intentional per the roadmap, but it needs S2 fixed and a note that it is unwired. **Fixed (R11):** S2 landed in R4 (`open` is non-destructive) and the module now opens with a "shipped but unwired" note listing the five preconditions for wiring it — app-chosen path, opt-in recovery, structured messages only, writes off the render thread, and a retention decision for `MAX_TRANSCRIPT_FILE_BYTES`. Kept, not deleted. | done |
| G4 | H | `pty.rs` | The "behaviourally identical to feeding every byte individually" claim was untested. Now pinned against an explicit byte-at-a-time reference feed comparing screen contents, cursor position, scrollback depth and the emitted response bytes: 26 hand-written adversarial cases (split and truncated CSI, oversized CSI, `ESC` at end of chunk, CPR mid-stream, OSC/DCS, mixed and invalid UTF-8) × 4 screen sizes × 8 chunk sizes, plus 64 seeded xorshift streams × 5 chunk sizes. No `proptest`. Verified by injecting two batching bugs, both caught. | done |
| G3 | H | new `fuzz/` | No fuzz targets. Added a `cargo-fuzz` crate in a top-level `fuzz/` with its own `[workspace]`, so normal builds, `cargo deny` and the MSRV job are untouched; it reaches the responder through the feature-gated `fuzz_feed_terminal_output` seam. Targets: `protocol_read_message` and `vt_response_detector` (arbitrary output, interleaved resizes, input-chosen sizes including the clamped floor). `.gitignore` already covered `fuzz/{target,corpus,artifacts}`; documented in `CONTRIBUTING.md`. Not in CI. | done |
| G6 | M | `ui.rs:2151-2168` | `draw_text` now dumps the whole buffer (better than v1's per-row grep) but every assertion is `.contains(...)`. Zero full-buffer equality, no 80×24 case. **Fixed (R6):** four `insta` snapshots (default 100x30, command palette, terminal with scrollback and selection, narrow 80x24) pin the whole buffer as a glyph grid *plus* a parallel per-cell style grid with a legend, so pane boundaries, header offsets and selection are asserted rather than only glyphs. Scenarios deliberately avoid the one `$HOME`-dependent pane (the chat "not started" hint prints `config_path()`); git branches are caller-supplied here, never probed. | done |
| G2 | M | `protocol/lib.rs:727,856-940` | Framing tests cover oversize and trailing bytes; still untested: truncated frame, `len == 0`, payload split across reads, malformed postcard. No `crates/protocol/tests/`. **Fixed (R11):** new `crates/protocol/tests/framing.rs` (10 tests) with a `Read` impl that yields N bytes per call — truncation at every prefix offset, `len == 0`, one-byte-at-a-time reassembly, EINTR retry, back-to-back frames in one stream, a short second frame after a good first, an oversize prefix rejected before the payload is touched, and 256 seeded postcard mutations (bit flip / truncate / grow) from a hand-rolled xorshift64\* — no `proptest`. | done |
| G5 | M | `storage.rs:381,900+` | Storage tests grew a lot, but no serde *shape*-error test and no backup-rename-failure test. **Fixed (R11):** `a_shape_error_backs_the_file_up_and_resets_to_a_fresh_state` (renamed by R7a to `…_and_reports_where_the_backup_went`; valid JSON, current version, `workspaces` a string) and `a_failed_backup_rename_aborts_the_reset_and_keeps_the_original_bytes` (parent chmod `0500`, so `renameat` returns `EACCES`). Both **pin current behaviour, which is questionable**: any shape error routes into the corrupt-backup-and-reset path and discards every workspace, silently as far as the user is concerned. Leniency and notification are **E11** (R7); the two tests above are the ones E11 must update when it changes the policy. | done |
| G9 | M | `mult-server.rs:51,1573` | `RAW_HISTORY_CHUNK_BYTES` is referenced by no test; the largest replay fixture is ~416 bytes — one chunk. Boundary cases untested. **Fixed (R11):** `replay_splits_history_at_the_chunk_bound_and_delivers_it_byte_for_byte` drives a real attach transaction at `CHUNK-1`, `CHUNK`, `CHUNK+1` and `3*CHUNK+7`, asserts the chunk count is `ceil(len/CHUNK)`, that no chunk exceeds the bound, that sequences are contiguous through the watermark, and that the concatenated delivery equals the source byte for byte; `an_empty_history_replays_as_a_bare_begin_and_end` covers the terminator. Note: only a *single* append larger than 64 KiB splits — the reader's 8 KiB buffer never reaches the bound. | done |
| G7 | M | `protocol/lib.rs:1437,1441`; `config.rs:248` | Two of the three env-mutating tests remain, including `set_var` on a process global raced by sibling tests. **Fixed (R11):** pure seams `socket_path_from(explicit, runtime_dir, user_component)` and `config_path_from(explicit, config_home)`, both keeping the fallback lazy so an override still works with no resolvable home; the env-reading wrappers just delegate. No test in the workspace mutates process-global env any more. | done |
| G11 | M | `pty_integration.rs:34,163+`; `agent.rs:435,499` | Embedded `sleep 1/2/3` under a 5 s cap; 2 s deadline polled at 10 ms. **Fixed (R11):** `INTEGRATION_TIMEOUT` 5 s → 30 s, `CHILD_REAP_TIMEOUT` 2 s → 15 s, agent deadline 2 s → 20 s polled at 5 ms; every wait already returns on its condition, so the happy path is unchanged (the suite still finishes in ~0.9 s). The timing `sleep`s are gone: `printf replayed; sleep 2` became a keep-alive loop, and the SIGHUP fixture's `sleep 1`/`sleep 3` became a FIFO handshake, so `after` is produced *because* the test released it, strictly post-hangup. `client_receives_scrollback_output_and_real_exit_from_server_pty` raced by construction — it needs a live `PtyOutput` after replay, but a command that finishes before Attach delivers everything as scrollback — and now blocks on a FIFO the test opens only once `start` (create **and** attach) has returned, so the second write is live output by construction. | done |
| G10 | M | `agent.rs:370-503` | Still exactly the 4 audited happy-path tests. **Fixed (R11):** five more — spawn failure (`NotFound`, not tracked as running, no orphan events), nonzero exit emits `Error` and never `Done`, the pipe reader survives invalid UTF-8, the 1024-slot queue delays a reader without losing a byte, and a dropped consumer releases a blocked reader instead of pinning a thread. The test module now states that the whole module is unwired Phase-3.4 scaffolding, so these are about being safe to wire up, not live behaviour. Only `cat` is used, so nothing here needs `$SHELL`. | done |
| G12 | M | `ui.rs:821,865,921` | vt100→ratatui adapter untested directly; no wide-cell case. **Fixed (R6):** `vt100_attributes_map_to_ratatui_modifiers`, `vt100_color_variants_map_to_reset_indexed_and_rgb` and `wide_cell_occupies_one_symbol_and_blank_successor`, plus D2's symbol-storage regression test (a base character with five combining marks, and `CellSymbol` at exactly 24 bytes and one byte past). The wide-cell test records a vt100 quirk found while writing it: a wide *continuation* cell reports `has_contents() == true` while its contents are empty, because the flag shares a byte with the length — so the adapter must key the symbol off the contents, not the flag. | done |
| G13 | L | `config.rs:224-368` | 9 tests, none for malformed JSON, unknown keys, or bad hex. Policy is settled in code, so these can be written directly. **Fixed (R7a):** 23 tests now. New: `malformed_config_json_is_an_error_not_a_silent_default` (asserts the `path:line:col` prefix and that serde's position is not printed twice), `unknown_config_keys_are_reported` (top level, `colorscheme`, and a `projects` entry), `invalid_color_strings_fall_back_to_the_default_palette_and_report`, `a_project_path_that_is_not_a_directory_is_a_warning_not_an_error`, `a_symlinked_config_names_the_path_and_the_workaround`, `a_rejected_config_directory_says_config_not_state`, and `the_config_flag_outranks_the_environment_and_the_default`. | done |
| G14 | L | `git.rs:6,37-52` | `current_branch` untested; only `first_nonempty_line` covered. | done |
| S9 | M | `flake.nix:30` | **New.** The `nix` CI job sets `MULT_SKIP_PTY_INTEGRATION=1`, so it reports 24 tests in 0.00 s — a fully green job with zero PTY coverage, and nothing anywhere asserts the integration tests actually ran. This is the unmet half of v1's G1. **Fixed (R11):** two independent locks. `MULT_REQUIRE_PTY_INTEGRATION=1` makes a requested skip a hard assertion failure, and `pty_integration_harness_runs_a_real_pty` prints `MULT_PTY_INTEGRATION_RAN` only after a real daemon has driven a real PTY. The `cargo` job (Linux **and** macOS) sets the variable, runs `--test pty_integration -- --nocapture`, greps the sentinel and asserts a ≥20 passed count, so neither a stray skip nor a `--skip` filter can go green. The Nix sandbox keeps its skip and now documents where the coverage is taken instead. | done |
| S10 | L | `flake.nix:27` | **New.** The flake sets `MULT_TEST_SHELL`, which only the skipped integration file reads, while the code that actually needs a shell in the sandbox (`mult-server.rs:2775`, `pty.rs:2487`) reads `$SHELL`, which the flake does not set. Latent trap for the next test that spawns a pane. **Fixed (R11):** the derivation now also sets `SHELL = "${pkgs.runtimeShell}"`, with a comment saying which of the two variables each reader uses. `nix flake check` still passes. | done |

**Fixed by `main`:** G1 (**verified empirically** — the harness returns `Result`, all 24 call sites
`.expect()`, and a sabotaged setup fails red), G8 (~33 server tests drive `handle_client` over a
real `UnixStream::pair`), G15 (`ensure_private_dir` breaks at the filesystem root; `nix flake
check` passes).

## H. CI, tooling, docs & release

| ID | Sev | Where | Item | Status |
|----|-----|-------|------|--------|
| S11 | M | `.github/workflows/ci.yml:44` | **New.** The MSRV job runs a bare `cargo check` while `rust-toolchain.toml` pins `1.94` — and the toolchain file beats `rustup default`. Whether the job tests 1.88 at all depends on `dtolnay/rust-toolchain` exporting `RUSTUP_TOOLCHAIN`. Add an explicit `+1.88`. (1.88 does currently compile — verified.) **Fixed (R12):** the job runs `cargo +1.88 check --locked …`, which outranks the toolchain file, and the claim was re-verified against the current tree (R1 and R6 landed) — `cargo +1.88 check --workspace --locked --all-targets --all-features` passes, so `rust-version = "1.88"` stands rather than being raised. | done |
| H3 | M | `justfile:37`; `README.md:67`; `CONTRIBUTING.md:26` | `just ci` = `fmt-check lint test audit`; CI additionally runs macOS, `cargo deny`, an npm typecheck, MSRV, and `nix flake check`. Both docs still misdescribe it. **Fixed (R12):** `ci` is now `version-check fmt-check lint test deny typecheck`, and the GitHub `cargo` job runs exactly that on Linux *and* macOS (the standalone `deny` job was dropped as redundant). `typecheck` degrades to a notice when `npm` or `extensions/node_modules` is absent so the gate still completes offline; CI runs the real `npm ci` version in its own job. README and CONTRIBUTING now enumerate the six steps and the four extra CI jobs. | done |
| H4 | M | `CONTRIBUTING.md:10,14` vs `flake.nix:60-68` | CONTRIBUTING promises `cargo-deny` from `nix develop`; the devShell has `cargo-audit` but not `cargo-deny`. **Fixed (R12):** `cargo-deny` and `cargo-llvm-cov` are in the devShell; `cargo-audit` was removed rather than kept alongside (see H10). Note the shellHook still prepends `$HOME/.cargo/bin`, so a rustup-installed copy shadows the flake's — recorded in ROADMAP.md as an open decision rather than changed here. | done |
| H5 | M | `.github/workflows/` | No release workflow, no tags, no binaries. **Fixed (R12):** `release.yml` builds Linux (gnu + musl) and macOS (x86_64 + aarch64) archives on a `v*` tag, each carrying **both** binaries plus both licences (the client resolves the daemon next to `current_exe()` and now ownership-checks it, so they must ship together). The tag is checked against the declared version and `just ci` runs *before* anything is published; the release is a draft until every target uploads. `docs/RELEASING.md` added. **No tag was cut — that is the owner's call.** | done |
| H7 | M | `README.md:129` vs `config.rs:40-65` | 3 of 12 colorscheme keys documented; `_nc` still unguessable. No `docs/CONFIG.md`. **Fixed (R12):** `docs/CONFIG.md` documents every top-level and colorscheme key with type, default and effect, sourced from R6's `config::DEFAULT_COLOR_SCHEME` (now the single definition) and from where each colour is actually used in `ui.rs`. It also records the two honest caveats — unknown keys are ignored silently and a bad colour falls back silently — both of which E6 will change. Linked from the README. | done |
| H8 | M | `docs/` | No troubleshooting guide. Messages must be re-quoted from current source. **Fixed (R12):** `docs/TROUBLESHOOTING.md` covers protocol-version mismatch after an upgrade, autospawn failing on a missing *or untrusted* adjacent `mult-server` (R4's ownership check landed mid-slice and is quoted as shipped), `.corrupt-*` backups, unsupported state versions and the state lock, peer-credential rejection, restoration without relaunch, a missing `pi`/`claude` binary, pane write-queue and daemon capacity refusals, a non-private runtime directory disabling status, and unresolvable HOME/XDG. Every message was grepped from source, not recalled. | done |
| H9 | M | `ci.yml`; `justfile` | No coverage measurement over ~19k lines. **Fixed (R12):** `just coverage` and a CI job run `cargo llvm-cov`. Measured baseline: **82.13% of lines** (18 150 lines, 3 243 uncovered) and 82.49% of regions. Best and worst: `model.rs` 95.60%, `protocol` 95.73%, `config.rs` 96.12%; `main.rs` 0% (binary entry point, unreachable from tests), `git.rs` 47.06% (G14), `paths.rs` 65.42%, `runtime.rs` 70.36% (F9 — binary-private, so tests cannot reach it). CI's `--fail-under-lines` is **75**, deliberately well below the baseline so unrelated work is not blocked. | done |
| H10 | L | `ci.yml:3-6,29,60` | No `schedule:`/`workflow_dispatch`; `cargo audit` and `cargo deny check advisories` still both present and redundant. **Fixed (R12):** a weekly `schedule:` (Mon 05:17 UTC) and `workflow_dispatch:` were added. The redundancy was resolved **in favour of `cargo deny`**, as `deny.toml` itself states: the `audit` recipe became `deny`, `cargo-audit` left the devShell and the CI tool list, and README/CONTRIBUTING say so. `just deny` still fails hard when `cargo-deny` is absent, mirroring the old `audit` behaviour. | done |
| H11 | L | `.github/` | No issue/PR templates, no CODEOWNERS. **Fixed (R12):** `ISSUE_TEMPLATE/bug_report.yml` (requiring terminal emulator, `$TERM`, OS and whether the kitty keyboard protocol is active — all four decide input encoding, per the CHANGELOG's history there), `ISSUE_TEMPLATE/feature_request.yml`, `ISSUE_TEMPLATE/config.yml` (blank issues off, security routed privately), `pull_request_template.md` (referencing `just ci`, plus protocol/state-version and docs checks), and `CODEOWNERS`. | done |
| H12 | L | `CHANGELOG.md` | No reference-link definitions, so both headings render literally; `[0.1.0]` undated; no tag. **Fixed (R12):** `[Unreleased]` and `[0.1.0]` link definitions added, and `[0.1.0]` dated `2026-05-19` from the first commit, with an explicit note that no tag was cut so the second link resolves only once one is. The `[Unreleased]` entries were not restructured. | done |
| H13 | L | `README.md:170-183`; `extensions/package.json:6` | Layout table omits `runtime.rs`, `git.rs`, `paths.rs`, `lib.rs`, `terminal_guard.rs`, `transcript.rs`; package.json still says the extension is embedded in `src/main.rs` (it is `runtime.rs:73`). **Fixed (R12):** the table was regenerated from the tree — all six missing files added, `main.rs` re-described as the entry point rather than the event loop, `runtime.rs` given the event loop it actually holds, plus `src/snapshots/`, `tests/` and `tests/fixtures/state/`. Modules are marked `(lib)` or not, so it now shows that `runtime.rs` and `terminal_guard.rs` are binary-private and unreachable from integration tests (F9's premise). `package.json` corrected to `src/runtime.rs`. | done |
| H14 | L | `CONTRIBUTING.md` | `MULT_SKIP_PTY_INTEGRATION` and `MULT_TEST_SHELL` documented nowhere. **Fixed (R12):** both documented in CONTRIBUTING under "Test environment variables", each with the trap that motivated S9/S10 spelled out (skipping reports green while testing nothing; `MULT_TEST_SHELL` is read only by the integration file, while production reads `$SHELL`). The README env table was rebuilt from a full audit of what the code reads: `MULT_SERVER_AUTOSPAWN`'s real accepted values, the XDG/HOME/SHELL variables it had omitted, `MULT_AGENT_CMD` marked **experimental and inert** rather than removed (E12), and the seven `MULT_AGENT_*` variables `mult` *sets* for its children listed separately. `TERM`/`COLORTERM` are set by the daemon, not read, and are described as such. | done |
| H15 | L | `.gitignore` | Missing `.claude/settings.local.json`, `*.corrupt-*`, editor dirs, and now `/fuzz/target`, `/fuzz/corpus`, `/fuzz/artifacts`. **Fixed (R12):** all of those, plus `src/snapshots/*.snap.new` (R6 added `insta`; a rejected snapshot must never be committed) and OS scratch. | done |
| H16 | L | `justfile` | No `install-hooks` recipe. **Fixed (R12):** `just install-hooks` writes a `pre-commit` running only `cargo fmt --all -- --check`, resolved through `git rev-parse --git-path hooks` so it works in a worktree. Formatting only on purpose — a hook slow enough to be worth bypassing gets bypassed. | done |
| H17 | L | 4 files | Four independent `0.1.0` literals; no `[workspace.package] version`, no `just version-check`, no `docs/RELEASING.md`. **Fixed (R12):** four literals down to three. `[workspace.package]` now holds `version`/`edition`/`rust-version`/`license`/`repository`, and both crates inherit them, so `crates/protocol` no longer declares its own. `flake.nix` and `extensions/package.json` cannot inherit and are guarded instead by `just version-check`, which also asserts the protocol crate still inherits and optionally pins an expected value — the release workflow passes the tag. Wired into `just ci`, so a drift fails locally before it can reach a tag. `docs/RELEASING.md` added. | done |
| S12 | L | `docs/` | **New.** `README.md:196` and `CONTRIBUTING.md:44` point at `docs/REMAINING_WORK.md` as the authoritative follow-up list, but it is a stale "Phase 3 partially implemented" handoff plan, and this backlog is linked from nowhere. Reconcile the two into one roadmap. **Fixed (R12):** new `docs/ROADMAP.md` is the single entry point, fronting this file (items) and `PLAN.md` (execution order), and README/CONTRIBUTING/templates point at it. `REMAINING_WORK.md` was **merged, not deleted** — its owner intent was carried across: the Phase 3 transcript-boundary contract (with the "hooks and PTY chunks are not message boundaries" rule and its restart completion test), the §6.3 open decisions on incomplete public paths, §6.4's extension-dependency migration, §6.6's devShell PATH question, and the Phase 7 projects are now ROADMAP.md sections; the per-phase rules became "Standing rules"; §6.7's release decision is answered by `RELEASING.md`. All three superseded documents (`REMAINING_WORK.md`, `BACKLOG-v1.md`, `PLAN-v1.md`) carry historical banners pointing at the live set. | done |

**Fixed by `main`:** H1 (MSRV job exists — see S11 for the caveat), H2 (`--locked` everywhere),
H6 (`docs/REMAINING_WORK.md` exists, so the dangling links resolve).
